# 專案地圖 Roadmap — 個人健康檢查管理平台

> 最後更新：2026-07-19（Sprint 16／E4-F3 串流問答引擎 PoC 2/2 已 commit／push／正式站部署驗證通過，E4 Epic 全數完成；已在正式站真實瀏覽器對正式站共用資料庫＋真實 OpenAI API 完整端到端操作驗證通過）

## 1. 一句話目標
讓 50–65 歲使用者把多年健檢資料變成可追溯的趨勢與可停止的行動計畫。

## 2. 全案進度總覽

| Epic | 名稱 | 進度 | Feature 完成 | 預估 Sprint |
|---|---|---|---|---|
| E1 | 平台與信任基座 | 🟢🟢🟢🟢⚪ | 4/5（E1-F1／E1-F2／E1-F4／E1-F5 ✅已部署；E1-F3 Google 登入後移） | 6 |
| E2 | 健檢資料入庫管線 | 🟢🟢🟢🟢 | 4/4（E2-F1／E2-F2／E2-F3／E2-F4 ✅已部署，Epic 全數完成） | 5 |
| E3 | 健康洞察呈現層 | 🟢🟢 | 2/2（E3-F2／E3-F1 ✅皆已部署，Epic 全數完成） | 2 |
| E4 | 知識庫與串流問答引擎 | 🟢🟢🟢 | 3/3（E4-F1／E4-F2／E4-F3 皆完成，Epic 全數完成，皆已部署驗證通過） | 4 |
| E5 | 健康行動閉環 | ⚪⚪⚪⚪ | 0/4 | 5 |
| E6 | 稽核基座與交付驗證包 | ⚪⚪ | 0/2 | 2 |

整體進度：已完成 Sprint **16 / 24**（粗估 17，精估 24，差異說明見 05_BACKLOG）

## 3. 目前所在位置
- 最近完成：**Sprint 16（E4-F3：SSE 串流問答引擎，PoC 2/2）已 commit（`9323781`）＋push＋正式站部署驗證通過（2026-07-19），E4 Epic 全數完成**：補齊 PoC 1/2 排除的四項——`/projects/[id]/chat` 對話頁 UI、`regenerate`（`messages.regeneratedFromMessageId`，A78）、頻率限制（C17，A79）、安全提示打磨（A81，僅顯示不攔截）。**修正 Sprint 15 遺留缺陷**：`runAssistantMessage()` 原無條件抓對話第一則訊息當提問，多輪對話下答非所問，改為沿 `regeneratedFromMessageId` 回溯＋抓最近使用者訊息的 `resolveQuestionText()`。**已在正式站真實瀏覽器對正式站共用資料庫＋真實 OpenAI API 完整端到端操作驗證**：建立對話→提問→串流顯示八段結構完整回答→引用清單正確顯示→重新產生取得新版本、UI 正確只顯示最新版本，皆在正式生產環境（`health-devkit.zeabur.app`）實測通過。全專案 140 測試（+5）／typecheck／lint／build 全綠
- 下一個：開 Sprint 17 DOR（依既定順序推進 E5-F1：健康行動計畫，PoC）

## 4. 接下來三步
1. 開 Sprint 17 DOR（E5-F1：健康行動計畫，PoC）
2. 剩餘 32 章節（約 681 頁）真實內容轉錄方式待決定；KB-018／KB-020 待決定處理時機（皆非阻塞）
3. E5 Epic（健康行動閉環）逐 Feature 推進

**OCR 排程最終拍板（2026-07-16）**：掃描 OCR 啟用（C4 flag）已移出 05_BACKLOG「Phase 2+」大雜燴。**PoC 完成（KB-023，86% 真實文件無文字層）後，PO 決定維持原計畫，OCR 仍排在 E2-F3 之後，不提前插隊**——優先把文字型管線＋手動輸入 fallback 做穩、資料模型穩定後再接 OCR，避免整合成本與風險。屆時另立 DOR，需先決定 OCR 供應商（自架 vs. 雲端 API，涉及成本與健檢文件送第三方的隱私考量），並將手機拍照特有挑戰（透視變形、光線、解析度、部分入鏡）納入設計考量，不只是掃描器品質輸入。

## 5. 目前最大風險與外部依賴
1. 🟢 E2-F2 表格抽取準確率（PoC 1/2＋2/2 皆完成並部署驗證通過，正式結案；A22 可行性、A26–A28 準確率調校根因修正皆已驗證，見 KB-023／KB-024。**殘留風險已從「準確率未知」限縮為「覆蓋率天花板可能只有一到兩成」（OCR 排程已拍板，維持原計畫）＋「語法上恰好合法的黏合巧合無法辨識」（KB-024 記錄的根本限制，非實作疏漏）**）
2. 🟢 E4-F3 引用驗證端到端（PoC 1/2＋2/2 皆完成並正式站部署驗證通過，E4 Epic 全數完成；已用真實 OpenAI API 呼叫＋正式站真實瀏覽器操作驗證成功——LLM 確實遵循結構化 ID 引用標籤格式，虛構／跨專案引用皆正確剔除，UI／`regenerate`／頻率限制／安全提示皆已打磨並在正式生產環境實測。**殘留範圍**：語意層面「claim 與引用段落一致」的深度核對（技術選型 §11.5，需額外 NLI 模型或二次 LLM 呼叫，非本輪 PoC 範圍）；安全把關仍為關鍵字掃描顯示層級，非自動攔截，見 A81）
3. 🟡 惡意檔案掃描缺口（KB-021，Sprint 6 已上線生效）：上傳功能僅以內容驗證＋大小限制為第一道防線，尚未接任何 AV 掃描服務；E2-F2 補上逐工作逾時防線（KB-022）擋解析器層級攻擊，但檔案內容層級掃描仍空白。**05_BACKLOG §49 footnote 要求的「E2-F3 完成後重新檢查」已於 Sprint 9（2026-07-17）完成：PO 決定維持原計畫，留到 E6-F2，不提前**
4. 外部：上線前台灣個資與醫療法律審查（E6-F2 硬門檻）

## 6. 里程碑

| 里程碑 | 內容 | 對應 Epic | 預估落點 | 狀態 |
|---|---|---|---|---|
| M1 | 可登入、建專案、填背景 | E1 | ~Sprint 6 | 🟢 達成（Sprint 5，提前） |
| M2 | 第一份報告入庫、看見趨勢（核心價值驗證） | E2＋E3 | ~Sprint 13 | ⚪ |
| M3 | 有來源的串流問答上線 | E4 | ~Sprint 17 | 🟢 達成（Sprint 16，2026-07-19） |
| M4 | 行動閉環完整（計畫→回報→檢討→分流） | E5 | ~Sprint 22 | ⚪ |
| M5 | MVP 交付（含法律審查） | E6 | Sprint 24 | ⚪ |
